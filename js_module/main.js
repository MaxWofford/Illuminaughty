var app = angular.module('Illuminatify');

app.controller('IntrueitiveController', ["$scope", "$routeParams", "$document", "$compile", "$timeout",
    function ($scope, $routeParams, $document, $compile, $timeout) {
        console.log(101);
    }]);
