var app = angular.module('Illuminatify', []);

app.controller('IlluminatifyController', ["$scope", "$document", "$compile", "$timeout",
    function ($scope, $document, $compile, $timeout) {
        console.log(101);
    }]);
